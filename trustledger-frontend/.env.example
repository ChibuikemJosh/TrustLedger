# VITE_BACKEND_BASE_URL: The URL of your external backend server on Render.
# Defaults to https://trustledger-1.onrender.com if not specified.
VITE_BACKEND_BASE_URL="https://trustledger-1.onrender.com"

# APP_URL: The URL where this frontend application is hosted (e.g. on Google Cloud Run).
# AI Studio automatically injects this at runtime with your active Cloud Run service URL.
# Used for self-referential links, CORS verification, and callback redirect matches.
APP_URL="https://ais-dev-dzuvhjmnahs2irggkrxjfv-933142538665.europe-west2.run.app"
